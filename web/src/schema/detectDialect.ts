export {
  detectDialect,
  DIALECT_DETECT_SCAN_LIMIT,
  DIALECT_DETECT_MIN_CONFIDENCE,
  DIALECT_DETECT_MIN_SCORE,
  DIALECT_DETECT_FALLBACK_ID,
  type DialectDetectionResult,
  type DetectedInputFormat,
} from '../../../src/utilities/detectDialect.ts';
